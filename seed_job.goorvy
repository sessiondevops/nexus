job('GIT_BRANCH_1') {
    scm {
        github('https://github.com/sessiondevops/nexus.git', 'development')
    }
    def releaseScript = readFileFromWorkspace('Jenkinsfile')
    steps {
        groovyCommand(releaseScript)
    }
    triggers {
        scm('*/1 * * * *')
    }
    println env.JOB_NAME
}
